[Unit]
Description=<%= @name %> Container
After=<%= @after %>

[Service]
ExecStart=/usr/bin/docker-<%= @name %>-start.sh
ExecStop=/usr/bin/docker-<%= @name %>-stop.sh
Restart=always

[Install]
WantedBy=local.target